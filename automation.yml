name: Reddit Monitor

on:
  schedule:
    - cron: "0 */4 * * *"
  workflow_dispatch:

jobs:
  scan:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    permissions:
      contents: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Run Reddit monitor
        run: node reddit-monitor.js

      - name: Commit updated opportunities
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/opportunities.json
          git diff --staged --quiet || git commit -m "chore: update opportunities $(date -u '+%Y-%m-%d %H:%M UTC')"
          git push
